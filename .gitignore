# Node.js
node_modules/
dist/
build/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript
*.tsbuildinfo

# Logs
logs
*.log
logs/*.log
midnight-level-db
coverage

# Test reports
**/reports

# Dependency directories
jspm_packages/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env

# MacOS
.DS_Store

# VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# IDEs and editors
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln

# Compact — generated compiler output is ignored by default (regenerate locally with
# `npm run compact`), EXCEPT contract/src/managed/attesta, which is committed on purpose:
# Render's static-site build environment doesn't have the Compact toolchain installed,
# so the hosted frontend build (see render.yaml) skips `npm run compact` and reuses this
# pre-compiled output instead. Regenerate and re-commit it whenever attesta.compact
# changes.
managed/
!contract/src/managed/
!contract/src/managed/attesta/
!contract/src/managed/attesta/**

.claude
.midnight-expert

# Midnight wallet state — per-network seeds, generated locally, never committed
# (deploy-engineer, Bloco 0 — Armadilha 3, contexto/Agents/deploy-engineer.md)
.midnight-state.json
.midnight-state.*.json

# Pipeline project folder — judge/strategy analysis for this Buildathon submission,
# must never reach the public repository (07-DOS-E-DONTS.md, "Nunca deixar a pasta
# do projeto... entrar em repo público")
contexto/

# Build trail (agent/human working log), kept locally for writing the docs site later —
# not meant for the public repo
feedback.md